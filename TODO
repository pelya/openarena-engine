Next release:
Кнопка «Джойстик» по умочанию все еще пересекается с элементами дашборда и возникающими сообщениями.
Cancel scanning button for server list.
When switching from Favorites to Local+Internet, servers are listed twice.
When waiting for answer from masterserver, GUI freezes.
Option to move weapon bar to the bottom.
Pressing Fire button sohuld not trigger events for other buttons if finger moves to them.
Remove resolution setting from video menu.
Option "Listen only" for voice chat.


Distant future:
4-players split-screen mode, to be taken from https://github.com/zturtleman/spearmint
Make weblinks and emails clickable in chat text and in server messages


Minor:
Q_vsnprintf inside bg_lib.c is buggy with %f output
Fix chat messages split incorrectly
