Next release:
Remove all aiming modes except "Tap to shoot".
Camera rotate button above the joystick does not work, it sends 'centerview' VM command.
Move all controls to the engine, so that VM from the PC version can be played.
Show PC servers by default, put them into the same list as Android servers.
Make sure all controls work when playing on PC servers.
"Tap to shoot" - make crosshair smaller when stopped shooting.

Distant future:
4-players split-screen mode, to be taken from https://github.com/zturtleman/spearmint
Make weblinks and emails clickable in chat text and in server messages

Minor:
Fix chat messages split incorrectly
When waiting for answer from masterserver on bad connection, GUI freezes
Voting breaks map rotation on dedicated server
